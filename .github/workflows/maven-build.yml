name: maven-build

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-main:
    secrets: inherit
    uses: dangernoodle-io/.github/.github/workflows/maven.yml@jae/coveralls-app
    with:
      maven-goals: verify
